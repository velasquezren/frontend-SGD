/**
 * Default environment — used by `ng serve`/`ng test` and any build that
 * doesn't request the `production` file replacement (see `angular.json`'s
 * `fileReplacements`). Local backend only; never deployed.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
